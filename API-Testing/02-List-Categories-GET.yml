info:
  name: 02 - List Categories - GET /category
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8081/category
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
