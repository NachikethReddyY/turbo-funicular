info:
  name: 07 - Register User - POST /users
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8081/users
  body:
    type: json
    data: |-
      {
        "username": "testuser",
        "email": "test@test.com",
        "password": "test123",
        "type": "Customer",
        "profile_pic_url": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
