info:
  name: 09 - Login Admin (John) — run first, copy token for A09 tests
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8081/users/login
  body:
    type: json
    data: |-
      {
        "email": "John@gmail.com",
        "password": "abc123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
