info:
  name: 09 - Login - POST /users/login
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8081/users/login
  body:
    type: json
    data: |-
      {
        "email": "terry@gmail.com",
        "password": "abc123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
