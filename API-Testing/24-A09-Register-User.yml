info:
  name: 24 - A09 Register User (screenshot backend terminal)
  type: http
  seq: 24

http:
  method: POST
  url: http://localhost:8081/users
  body:
    type: json
    data: |-
      {
        "username": "newuser123d4",
        "email": "newuserd1@example.com",
        "password": "testpass123",
        "type": "Customer",
        "profile_pic_url": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
