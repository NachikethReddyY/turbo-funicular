info:
  name: 19 - SQL Injection - GET /users/1 OR 1=1 (VULNERABILITY)
  type: http
  seq: 19

http:
  method: GET
  url: http://localhost:3001/users/1 OR 1=1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
