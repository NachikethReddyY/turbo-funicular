info:
  name: 12 - Expose All Users (No Auth) - GET /users (VULNERABILITY)
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:3001/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
