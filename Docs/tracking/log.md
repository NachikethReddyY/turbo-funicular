---
title: Cryptix — Weekly Progress Log
author: Team
date: 2026-06-08
---

# Project Progress Log

## Title: Cryptix — Weekly Progress Log
**Updated:** 2026-06-21
**Repo:** [https://github.com/NachikethReddyY/turbo-funicular](https://github.com/NachikethReddyY/turbo-funicular)

## Team

| Person        | Full Name         | GitHub          | Assigned OWASP | Issue                     |
| ------------- | ----------------- | --------------- | -------------- | ------------------------- |
| **Nachiketh** | Nachiketh Reddy Y | NachikethReddyY | A01 + A09      | #124 / #128 / #132 / #136 |
| **Keefe**     | Keefe in 4Tech    | —               | A07            | #125 / #129 / #133 / #137 |
| **Mike**      | Mike Franco Abat  | Mike-Franco     | A03            | #126 / #130 / #134 / #138 |
| **Sitt**      | Sitt              | **SINING-NEO**  | A04            | #127 / #131 / #135 / #139 |

> Each member is responsible for logging their own weekly progress. Nachiketh maintains the parent tracker issues.

## Week 0 (02 Jun – 08 Jun)
**Parent issue:** #120
| Person | What Was Done | Issues Faced | Plan to Improve / Fix |
| --- | --- | --- | --- |
| **Nachiketh** (Nachiketh Reddy Y) — A01 + A09 | Attended team meeting on 02-Jun (see `Meeting/02-Jun-26.md`) where we decided the final five OWASP categories and split ownership. I was assigned A01 (Broken Access Control) and A09 (Security Logging & Monitoring). Mapped all 19 API endpoints by reading through `controller/app.js` line by line — checked each route for `verifyToken` middleware, HTTP method, and input sources (params/body/query). Listed every SQL query across all 5 model files and noted which use parameterized `?` vs string interpolation `${}`. Found 18 candidate security flaws across both categories. For A01: no auth on 13/14 endpoints, IDOR on user data (`GET /users`, `GET /users/:userid` returns passwords), game deletion without ownership, client-supplied `type` field on registration (anyone can register as admin). For A09: zero structured logging anywhere, no audit trail for sensitive operations, no failed-login tracking, 403 denials in `verifyToken.js` are silently dropped. Documented everything in `Docs/week-0-nachiketh-scouting.md` with exploit scouting notes, code evidence (file paths + line numbers for every finding), and fix directions. Set up local dev environment: MySQL + PostgreSQL with `nr` user, imported `spgames_SC.sql` schema, configured single-port server on `:3001` that serves both API and frontend. Installed DataGrip, Bruno, OrbStack, kubectl + K9s. | Finding all 19 endpoints required reading 679 lines of `app.js` manually — there is no OpenAPI spec or route registry so I had to cross-reference every `app.get()`, `app.post()`, and `app.delete()` call manually. Could have missed one if I skimmed too fast. Three SQL queries use string interpolation (`users.js:101`, `game.js:159`, `game.js:305`) but they're surrounded by longer blocks of parameterized queries — easy to miss if you don't check every SQL string individually. The frontend and backend ran on separate ports (3001 and 8081). The frontend server also rejects non-GET requests. Had to modify the backend to serve frontend static files so everything works on one port. Git pager opened `git branch` output in less instead of printing to terminal — fixed with `git config --global pager.branch false`. | **Week 1 goals:** • Write a Bruno PoC request that registers a user with `"type": "admin"` and capture screenshot evidence • Draft fix for `POST /users` — either whitelist `type` to `["Customer", "admin"]` or remove it from the registration endpoint entirely (default new users to `"Customer"`) • Add `verifyToken` middleware to write endpoints first (`POST/DELETE`), then read endpoints exposing sensitive data • Create a logging utility with `winston`/`pino` and add structured log calls to `verifyToken.js` (log denied tokens) and `app.js` (log request method + path + userid) • Coordinate with Keefe on bcrypt hashing — the plaintext passwords in `users` table overlap with his A07 login flow analysis |
| **Keefe** (A07) | Attended the team meeting on 02-Jun and was assigned A07 – Identification and Authentication Failures. Began by investigating how user credentials were stored and processed within the application. Reviewed users.js to understand registration and login workflows and traced how user information was inserted into and retrieved from the database. Examined database tables using MySQL Workbench to verify how credentials were stored and compared database contents with backend logic. Investigated the relationship between configuration files, SQL queries, and authentication mechanisms to understand the complete authentication flow. This groundwork helped identify potential weaknesses and provided the necessary understanding required for future vulnerability assessment. | One of the most difficult challenges was troubleshooting HTTP headers and server responses. The framework often returned generic error messages, making it difficult to determine the root cause of issues. To overcome this, raw database errors were examined during development to identify SQL issues, authentication failures, configuration mistakes, and invalid request headers. This improved debugging efficiency and strengthened understanding of backend systems, HTTP request-response handling, and effective error reporting practices | Inspect JWT config (`auth/verifyToken.js`, `config.js`), trace password storage and login flow, document missing auth protections |
| **Mike** (A03) | Reviewed the backend for SQL injection and mapped all query locations across `model/users.js` and `model/game.js`. Identified the unsafe string-interpolated queries in `GET /users/:userid`, `POST /game`, and the `updateGame()` helper, then documented the findings in `Docs/week-0-mike-scouting.md` and `Docs/report.md`. | No major blockers. The main challenge was separating the few unsafe SQL strings from the parameterized queries so the report only included real A03 sinks. | **Week 1 goals:** Capture a safe proof-of-concept request for the strongest injection points, add screenshots or response evidence |
| **Sitt** (A04) | Attended team meeting on 02-Jun and was assigned **A04 (Insecure Design)** (#127 / #131 / #135 / #139). Synced local repo with `origin/main` (18 commits behind) and ran `pnpm install` for `Assignment/BackEndServer`. Reviewed admin/security flow across `admin.html`, `addNewCategory.html`, `addNewPlatform.html`, `addNewGame.html`, and `controller/app.js`. Found that admin access is enforced only in frontend JavaScript (`checkAdmin()` → `GET /CheckRole`), while backend admin routes (`POST /category`, `/platform`, `/game`, `DELETE /game/:id`) have no `verifyToken` or role checks. Identified 12 A04 candidate flaws including client-controlled `type` on registration, inconsistent role values (`user` / `Customer` / `Admin`), missing server-side input validation, remember-me storing plaintext password in `localStorage`, and review endpoint trusting URL `uid` instead of JWT. Documented seven frontend-only bypass test cases (curl/HTTP) and validation gap matrix. Wrote full scouting report in `Docs/week-0-sitt-scouting.md` following the team template, with exploit notes, code evidence, fix direction, and secure route design target. | On Windows, lowercase `docs/` and uppercase `Docs/` are the same folder — had to keep the report in `Docs/` to match the repo and avoid overwriting team files during `git pull`. Backend port is inconsistent: older assignment code references `:8081` but updated README/setup uses a single server on `:3001` — need team confirmation before running bypass tests. Several findings overlap with Nachiketh's A01 (unprotected endpoints, IDOR) and Keefe's A07 (plaintext passwords) — need to frame write-ups around **design root cause** (missing server-side controls by architecture) rather than duplicate their implementation-focused findings. Bypass tests are documented but not yet executed locally — still need MySQL running and servers started to capture actual pass/fail results. | **Week 1 goals:** • Run all seven bypass test cases (TC-01–TC-07) against local API and record results • Capture screenshots: admin UI lock vs successful direct API bypass • Create Bruno/curl PoC for top 3 findings (open POST /category, register as Admin, review IDOR) • Draft secure-design middleware chain (`verifyToken` → `requireAdmin` → `validateInput`) for admin routes • Agree canonical role enum with team (`Customer` vs `user` vs `Admin`) • Coordinate with Nachiketh on A01 overlap and Keefe on password/session design |

## Week 1 (09 Jun – 15 Jun)
**Parent issue:** #121
| Person | What Was Done | Issues Faced | Plan to Improve / Fix |
| --- | --- | --- | --- |
| **Nachiketh** (Nachiketh Reddy Y) | **A01 — Broken Access Control.** Assessed all protected API routes and documented four findings: missing authentication/authorisation, plaintext password exposure, SQL injection, and hardcoded JWT secret. Tested exploits with Bruno (unauthenticated admin register, open DELETE, user data exposure, SQLi payloads). Implemented fixes: `verifyToken` + `requireAdmin` middleware on sensitive routes, hardcoded `type = 'user'` on registration, parameterised SQL queries, `JWT_SECRET` from environment, password column removed from API responses. Removed sensitive `console.log` from `verifyToken.js`. Wrote A01 sections of the vulnerability report with code evidence and remediation notes. | Bruno tests needed admin Bearer token after auth middleware was added. `requireAdmin` initially failed for seed users because DB stores `Admin` with capital A — fixed case-insensitive role check. | **Week 2 goals:** Begin A09 logging and monitoring assessment; capture before-fix evidence before applying A09 code changes |
| **Keefe** (Keefe in 4Tech) | Continued working on `report.md` and explored secure coding practices. Reviewed `login.html` and found the “Remember Me” feature stores and displays the user’s password in the browser via client-side storage — an A07 vulnerability. Explored AWS Secrets Manager and AWS Cognito as mitigations and implemented a Cognito-based authentication flow. Improved understanding of JWT-based authentication where secure tokens are issued after login instead of storing passwords. | AWS services were initially unfamiliar (IAM roles, Cognito configuration, CLI commands). Difficulty implementing bcrypt in `user.js` caused HTTP 500 errors with limited error visibility. Integrating Cognito required refactoring existing auth without breaking frontend login. | Fix remaining vulnerabilities; strengthen `register.js` and `login.js` with secure hashing and input validation; refactor auth to rely on Cognito JWT tokens; improve report documentation format |
| **Mike** (Mike Franco Abat) | Continued the A03 review by reading through all backend model files and mapping the SQL queries in users.js, game.js, category.js, platform.js, and review.js. Separated the parameterized queries from the unsafe plain-text SQL strings and confirmed that the main injection sinks are GET /users/:userid, POST /game, and the updateGame() helper. Tested GET /users/:userid in Postman and captured a successful PoC showing unintended user data exposure, including the password field. Also tested POST /game using a single-quote payload in title, which produced a 500 Internal Server Error and confirmed that the insertGame() query is vulnerable to SQL injection. Documented the findings in mike-report.md, compared the vulnerable code against the fixed parameterized version, and prepared screenshot evidence for the report. | The main challenge was distinguishing the true injection sinks from the many correctly parameterized queries. The POST /game testing also required troubleshooting multipart form-data and file upload handling before the request worked correctly, so it took some time to get a clean PoC and response screenshot. | Finish attaching the screenshots to the report, keep the A03 section consistent with the rest of Docs/report.md, and decide whether updateGame() should remain a secondary code-only finding or be treated as supporting evidence only. |
| **Sitt** (A04) | Turned the Week 0 A04 scouting notes into the formal report content for insecure design. Wrote the A04 detailed finding on frontend-only admin access control, showing how `checkAdmin()` and hidden admin UI elements are insufficient when backend routes are not designed to enforce roles. Wrote the A04 brief finding on client-controlled role assignment during registration, explaining how a tampered `type` field could allow admin self-registration. Reviewed the current backend fixes (`verifyToken`, `requireAdmin`, server-assigned `type`) and aligned the write-up with the team's report format. Pushed the work on the `Week1-Sitt` branch for review. | Needed to keep the A04 explanation separate from Nachiketh's A01 work because both discuss unprotected endpoints. The main challenge was framing the issue as a design failure (security placed in the frontend and missing server-side trust boundaries) rather than simply repeating broken access control. Also had to match the team's report style with clear sections for flaw type, code evidence, exploitation, recommendations, and fixed code. | **Week 2 goals:** Create a separate Sitt report under `Docs/reports`, add screenshot evidence for the A04 findings, and apply any remaining small code fix inside A04 scope. In particular, check whether the registration page still exposes the `type` field even though the backend now assigns the role server-side. |

## Week 2 (16 Jun – 22 Jun)
**Parent issue:** #122
| Person                            | What Was Done | Issues Faced | Plan to Improve / Fix |
| --------------------------------- | ------------- | ------------ | --------------------- |
| **Nachiketh** (Nachiketh Reddy Y) | **A09 — Security Logging & Monitoring Failures.** Assessed logging and monitoring gaps after A01 fixes: no structured audit trail, `console.log(err)` leaking SQL details to stdout, username/email enumeration via duplicate-registration messages, and review impersonation with no forensic record. Captured before-fix evidence with Bruno and backend terminal (registration, SQL errors, enumeration, impersonation) plus VS Code code screenshots. Implemented A09 fixes: `securityLog.js` for JSON audit lines and safe error output, generic duplicate error message, review ownership validation, audit events on login/register/delete/review. Completed `Docs/reports/nachiketh-report.md` with embedded A09 screenshots. Reorganised project docs into guides/reports/archive folders and updated Bruno collection auth workflow. | After A01 auth was applied, A09 tests required admin JWT on register/game routes. Impersonation test needed correct user ID in URL (Terry posting as user 3, not user 1). Post-fix verification screenshots still pending. | **Week 3 goals:** Capture after-fix screenshots for A01 and A09; finalise report submission |
| **Keefe** (Keefe in 4Tech)        |       Refined and structured the A07 security write-up, improving README formatting with proper headings, separators, and clearer explanation of the recommendations section. Used Postman to explore backend API endpoints and test authentication and authorization behavior. Identified a security vulnerability involving an unsecured endpoint exposing sensitive user information. Used Snyk AI to generate a report.html file to document and consolidate previously identified vulnerabilities. Set up AWS Cognito and created a new user pool in the AWS Learning Lab to explore cloud-based authentication and identity management services. Identified and documented a session management issue related to session hijacking via client-side token handling.        |      Required deeper understanding of backend JWT authentication flow. Needed clarification on proper vulnerability documentation format for formal reporting. Initial difficulty interpreting Snyk AI vulnerability reports.        |           Continue refining security documentation for consistency across all findings. Further explore AWS Cognito authentication flows. Improve accuracy in identifying and validating vulnerabilities through testing. Enhance report quality to align with OWASP-style security standards.            |
| **Mike** (Mike Franco Abat)       | Finalised the A03 injection work for the assignment. Expanded `Docs/mike-report.md` with a stronger executive summary, exploit flow, database impact, code evidence, fix snippets, and testing notes for `GET /users/:userid`, `POST /game`, and the code-level `updateGame()` sink. Added the screenshot references in `Assets/Mike/` and replaced the placeholder tags in `Docs/report.md` with real image links. Patched `Assignment/BackEndServer/model/users.js` and `Assignment/BackEndServer/model/game.js` so the vulnerable queries now use parameterized placeholders instead of string interpolation, then verified both files with `node --check`. | Needed to keep the report and source code in sync while working around sparse-checkout state and the Windows filename issue from `Week3-Mike`. Also had to keep `updateGame()` framed correctly as a code-level sink because there is no direct controller route calling it. | Finish a final proofread, then stage and commit the A03 report and backend fixes. If needed, retest the payloads once more and confirm the final branch state before push. |
| **Sitt**                          | Created `Docs/reports/sitt-report.md` using the vulnerability report template and added A04 evidence screenshots under `Assets/Sitt/a04/`. The report documents two findings: frontend-only admin access control and client-controlled role assignment during registration. Added screenshot-style evidence for the frontend `checkAdmin()` flow, the original vulnerable admin route design, the corrected `verifyToken + requireAdmin` middleware design, the client-controlled `type` payload, the server-side role assignment fix, and a safe direct API bypass request. Updated `Docs/reports/README.md` so the Sitt report appears in the report index. Also applied a small A04 code fix in `Assignment/FrontEndServer/Public/register.html` by removing the `Account type` dropdown and removing `type` from the registration request payload, so the frontend no longer suggests that users can control their own role. | The latest `origin/main` already contained several backend fixes from the team, so I had to avoid duplicating or overwriting them. The remaining fix inside my scope was mainly the frontend registration role field. Another issue was the Windows sparse-checkout setup because some remote asset filenames contain invalid Windows characters, so local testing and updates needed to skip those paths. Live browser/API screenshots were not captured yet; current report images are code/request evidence generated from the reviewed files. | **Week 3 goals:** Run the web app locally with MySQL, backend, and frontend, then capture live evidence for the A04 tests. Verify that registration no longer sends `type`, unauthenticated `POST /category` is blocked, customer tokens cannot access admin routes, and admin tokens can still perform admin actions. Add live curl/Bruno response screenshots to `Assets/Sitt/a04/` if time allows, then update the report with before/after verification notes. |

## Week 3 (23 Jun – 29 Jun)
**Parent issue:** #123
| Person                            | What Was Done | Issues Faced | Plan to Improve / Fix |
| --------------------------------- | ------------- | ------------ | --------------------- |
| **Nachiketh** (Nachiketh Reddy Y) |               |              |                       |
| **Keefe** (Keefe in 4Tech)        |               |              |                       |
| **Mike** (Mike Franco Abat)       |               |              |                       |
| **Sitt**                          |               |              |                       |
